项目及项目背景和我的责任：

       “价敏”的"免费底tab”项目，是“价敏一条龙”的重点项目，第一个项目。主要能力是提供“免费项”的内容给“价敏”用户。对标的是字节系的“番茄小说”App。为了能够满足开发效率和灵活上线的特点，选择RN技术方案来实现。

        我主导了RN项目的开发和上线，以及性能优化和完善监控报错。

 困难和挑战：

       在此之前没有RN项目在底tab的位置展示过，在新的场景，很多沉淀方法需要重新验证，性能指标和监控报错要求异常严格。样式及交互方式复杂多样，模块众多，具有很大的开发量。

  过程&决策&行动：
    案例1(解决问题)：
问题和发现：“分类墙模块”是“横向跑马灯”的效果，支持手势横向滚动。在IOS设备上，页面的所有点击事件都不响应了。
定位问题：滚动效果是通过每一帧之前调用scrollTo来调整位置来实现的。scrollTo在IOS端会被认为是列表在滚动，IOS为了优化滚动时的渲染速率，把事件都阻止了。scrollTo一般认为是不需要响应事件的。
行动：使用`disableScrollViewPanResponder={true}`这个冷门属性，把点击事件打开，并且只有在“分类墙模块”在可见区域时才滚动。
结果：问题解决，并且兼具了IOS本身的渲染速率。
    案例2(性能优化和完善报错监控)：
发现问题：<1>性能：页面打开速率慢，空白时间达到2.5s以上；<2>报错监控：目前的大部分RN项目只有渲染阶段错误时触发的报错页面，需要完善。
定位问题：<1>按照渲染顺序，逐一排查。使用charles查看接口速度，发现超过2s。<2>需要增加“完善报错上报”、“JS报错监控”，报错后”重试渲染页面“。
行动：<1>协商服务端，接口每次只返回4个模块，以减少接口时间，并加上骨架屏，提升视觉体验。<2>增加“完善报错上报”、“JS报错监控”，报错后”重试渲染页面“。
结果：<1>页面空白时间降低，在960ms左右。 <2> 监控报错更加全面，有重试机制。
总结：

       针对RN技术栈，有了深入的认识，沉淀了通用代码，比如“自动轮播功能”，“跑马灯功能”，也完善了监控报错体系。同时在发现问题后，积极跟上下游协商，把问题解决在上线前，也提升了沟通能力。主导了“免费底tab”RN项目的开发和上线。